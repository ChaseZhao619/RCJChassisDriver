# 安全策略

本项目涉及会驱动真实硬件的固件和上位机代码。安全问题不仅包括传统软件漏洞，也包括可能导致机器人失控、执行器误动作或硬件损坏的问题。

## 支持范围

当前安全策略适用于默认分支和正在维护的开发分支。历史分支或实验分支不保证获得安全修复。

## 应报告的安全问题

请优先私下报告以下问题：

- 非法串口命令、坏 CRC、缺失参数或越界参数仍可能触发执行器。
- 底盘可能在未启用运动、IMU 无效或停止命令后继续运动。
- 默认测试宏、默认参数或启动流程可能导致上电后自动危险动作。
- Flash 参数写入、阈值保存或复位命令可能被误触发。
- 上位机错误路径、错误地图比例或异常状态可能自动发送危险命令。
- 文档或示例中包含可直接导致硬件损坏的错误步骤。

## 不属于安全漏洞的情况

以下问题通常应使用普通 issue：

- 普通编译失败或依赖缺失。
- 文档错别字、链接失效或表达不清。
- 已知硬件损坏、接线错误、供电不足。
- 需要调参但没有失控风险的问题。

## 报告方式

如果问题可能导致实车失控、执行器误动作或设备损坏，请不要在公开 issue 中贴出完整危险复现步骤。建议先联系维护者，或提交只描述影响范围的 issue，并说明愿意私下提供细节。

报告中建议包含：

- 受影响模块：固件、BSP、上位机、协议、文档或构建配置。
- 影响范围：底盘运动、执行器、Flash、MCU 复位、传感器判断等。
- 测试环境：是否架空、是否低速、是否隔离负载、供电和硬件版本。
- 复现条件的安全摘要，不包含可直接诱发危险动作的完整步骤。
- 建议修复方向或临时规避措施。

## 响应原则

维护者会优先处理可能导致硬件危险的问题。确认问题后，修复通常应包含：

- 参数校验或状态机保护。
- 安全默认值。
- 文档警告。
- 必要时更新 issue/PR 模板，防止同类问题再次发生。
